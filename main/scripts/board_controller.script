local message_list = {}
function on_message(self, message_id, message)
	if message_id == hash("stack") then
		print("In board controller")
		message_list = message
		for i=1, table.maxn(message_list) do print(message_list[i]) end
	end
end